Office.onReady(() => { document.getElementById("insertTextButton").addEventListener("click", () => { Word.run(async (context) => { const doc = context.document; doc.body.insertText("Clause inserted!", Word.InsertLocation.end); await context.sync(); }); }); });
